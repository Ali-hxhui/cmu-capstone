.env
__pycache__/
.venv/
*.pyc
.DS_Store

# Commit this round's collection runs and the cleaned dataset.
# Older local pilots stay out of git.
data/*
!data/runs/
data/runs/*
!data/runs/batch-*/
!data/curated/
data/curated/*
!data/curated/screening-*/

# Generated batch slices (regenerate with assign_collection_batches.py)
search_terms/batches/*.csv
search_terms/batches/transcript_chunks/

# Secrets and local credentials
*.log
Backup-codes*.txt
backup-codes*.txt

# Personal bilingual / Chinese notes (English docs live at repo root)
local/

# Local IDE type-check config (optional; not needed to run the pipeline)
pyproject.toml

# Project admin files (PDFs, slides, spreadsheets) — keep on shared drive
*.pdf
*.xlsx
*.docx
*.pptx
Slide4_Presentation_Script.md
Meeting_Run_Presentation_Script.md
Fall 2026 AHN Cancer Education Project-selected/
