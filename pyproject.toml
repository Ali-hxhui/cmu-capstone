[project]
name = "cmu-capstone"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "certifi>=2024.0.0",
    "youtube-transcript-api==1.2.4",
]

[tool.basedpyright]
venvPath = "."
venv = ".venv"
